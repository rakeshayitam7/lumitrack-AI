import './globals.css';

export const metadata = {
  title: 'LumiTrack — AI Virtual Camera Tracking',
  description: 'SIH26169 AI-assisted virtual camera tracking system for mobile FSOC terminals.'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
